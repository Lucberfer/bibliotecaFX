<?xml version="1.0" encoding="UTF-8"?>

<?import javafx.scene.control.*?>
<?import javafx.scene.layout.*?>

<BorderPane xmlns:fx="http://javafx.com/fxml" fx:controller="org.example.bibliotecafx.view.controller.SociosController">
    <top>
        <HBox spacing="10" padding="10">
            <Label text="Buscar:"/>
            <TextField fx:id="tfBuscarNombre" promptText="Nombre"/>
            <TextField fx:id="tfBuscarTelefono" promptText="Teléfono"/>
            <Button text="Buscar" onAction="#onBuscar"/>
            <Button text="Refrescar" onAction="#onRefresh"/>
        </HBox>
    </top>
    <center>
        <TableView fx:id="tablaSocios">
            <columns>
                <TableColumn fx:id="colId" text="ID" prefWidth="50"/>
                <TableColumn fx:id="colNombre" text="Nombre" prefWidth="150"/>
                <TableColumn fx:id="colDireccion" text="Dirección" prefWidth="150"/>
                <TableColumn fx:id="colTelefono" text="Teléfono" prefWidth="100"/>
            </columns>
        </TableView>
    </center>
    <bottom>
        <VBox spacing="10" padding="10">
            <Label text="Agregar / Modificar Socio"/>
            <GridPane hgap="10" vgap="10">
                <columnConstraints>
                    <ColumnConstraints halignment="RIGHT"/>
                    <ColumnConstraints/>
                </columnConstraints>
                <Label text="Nombre:" GridPane.rowIndex="0" GridPane.columnIndex="0"/>
                <TextField fx:id="tfNombre" GridPane.rowIndex="0" GridPane.columnIndex="1"/>

                <Label text="Dirección:" GridPane.rowIndex="1" GridPane.columnIndex="0"/>
                <TextField fx:id="tfDireccion" GridPane.rowIndex="1" GridPane.columnIndex="1"/>

                <Label text="Teléfono:" GridPane.rowIndex="2" GridPane.columnIndex="0"/>
                <TextField fx:id="tfTelefono" GridPane.rowIndex="2" GridPane.columnIndex="1"/>
            </GridPane>
            <HBox spacing="10">
                <Button text="Agregar" onAction="#onAgregar"/>
                <Button text="Modificar" onAction="#onModificar"/>
                <Button text="Eliminar" onAction="#onEliminar"/>
                <Button text="Limpiar" onAction="#onLimpiar"/>
            </HBox>
        </VBox>
    </bottom>
</BorderPane>
